/* ============================================================
   BEL-AIR — CONFIGURATION
   Modifie uniquement les valeurs ci-dessous.
   ============================================================ */

// Numéro WhatsApp professionnel (format international, SANS "+" ni espaces)
// Exemple RDC : 243983016575
const WHATSAPP_NUMBER = "243983016575";

// Nom de la boutique (utilisé dans les messages WhatsApp et le titre du site)
const SHOP_NAME = "BEL-AIR";

// La connexion administrateur est désormais gérée par Firebase Authentication.
// Pour créer ou modifier le compte admin : Console Firebase → Authentication
// → onglet "Users" → "Add user" (email + mot de passe).
